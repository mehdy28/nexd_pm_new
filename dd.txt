Here are the updated files to integrate the Tiptap editor for the task description.

### `graphql/resolvers/taskResolver.ts`

The `description` field type in the input interfaces has been changed from `string? | null` to `any` to correctly handle the JSON object from the GraphQL layer.

```typescript



